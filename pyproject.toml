[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openclaw-iphone-ops"
version = "0.1.1"
description = "Reusable primitives for controlling USB-connected iPhones from OpenClaw agents."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.urls]
Repository = "https://github.com/unblocklabs-ai/openclaw-iphone-ops"
Issues = "https://github.com/unblocklabs-ai/openclaw-iphone-ops/issues"

[project.scripts]
openclaw-iphone = "openclaw_iphone.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
